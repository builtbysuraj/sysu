export type SortMappableType = [
  string,
  {
    category: string
    description: string
    id: number
    subject: string
    timeStamp: string
  }
]

export type ParagraphType = (string | number)[]
